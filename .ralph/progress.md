- item: bounded request recovery state. decisions: added request-scoped recovery state/reset in `src/index.ts`, pure state helpers in `src/recovery-state.ts`, and clamped `PTC_AUTO_RECOVER_MAX_ATTEMPTS` to `0..1`; left failure classification and recovery prompt wiring for later items. files changed: `src/index.ts`, `src/recovery-state.ts`, `src/contracts/execution-types.ts`, `src/contracts/settings.ts`, `src/utils.ts`, `test/index.test.ts`, `test/recovery-state.test.ts`, `test/utils.test.ts`, `.ralph/items.json`, `.ralph/progress.md`. verification: `npm run build` ✅, `npm test` ✅. next: implement deterministic async-only failure classification and the minimal recovery prompt builder, then connect qualifying first-attempt failures to this state.
- item: deterministic async failure classification. decisions: added pure recovery classifier/prompt builder in `src/recovery-classifier.ts`; classify direct un-awaited helper calls as `missing-await`, coroutine iteration/indexing/unpacking misuse as `async-wrapper-iterated`, and keep unrelated SyntaxError/NameError at `null`; prompt text stays byte-stable and minimal. files changed: `src/recovery-classifier.ts`, `test/recovery-classifier.test.ts`, `.ralph/items.json`, `.ralph/progress.md`. verification: `npm run build` ✅, `npm test` ✅. next: wire the classifier into the request lifecycle so a qualifying first failure appends exactly one targeted recovery correction without broadening path semantics.
- item: async-only recovery lifecycle integration. decisions: queued one ephemeral `ptc-recovery` context append for the next LLM call after a first qualifying `PtcPythonError`, kept recovery bounded to a single request-scoped attempt, and left zero-match path failures ineligible. files changed: `src/index.ts`, `test/index.test.ts`, `.ralph/items.json`, `.ralph/progress.md`. verification: `npm run build` ✅, `npm test` ✅. next: add request/tool result telemetry for routing, recovery attempts, and terminal state without introducing any persistent sink.
- item: ephemeral request telemetry. decisions: attached additive `telemetry` and `recovery` metadata to successful `code_execution` result details, tracked auto-routing/first-path in request state, and kept telemetry in-memory/tool-result scoped only with no new persistence path. files changed: `src/index.ts`, `src/recovery-state.ts`, `src/contracts/execution-types.ts`, `test/index.test.ts`, `test/recovery-state.test.ts`, `.ralph/items.json`, `.ralph/progress.md`. verification: `npm run build` ✅, `npm test` ✅. next: implement the JSON eval corpus/schema so routing and recovery expectations can be benchmarked deterministically.
- item: JSON eval corpus/schema. decisions: added a deterministic eval-case validator in `src/eval-cases.ts` and seeded `.pi/evals/ptc/cases/*.json` for positive routing, direct negatives, mutation negatives, and async recovery scenarios; malformed cases now fail with stable validation errors in `test/eval-cases.test.ts`. files changed: `src/eval-cases.ts`, `test/eval-cases.test.ts`, `.pi/evals/ptc/cases/*.json`, `.ralph/items.json`, `.ralph/progress.md`. verification: `npm run build` ✅, `npm test` ✅. next: implement the benchmark runner that consumes these case files, emits deterministic JSON results, and compares provider-model baselines.
- item: benchmark runner. decisions: added deterministic benchmark run/result/baseline helpers in `src/benchmark-runner.ts` plus a CLI entry in `src/run-benchmarks.ts`; seeded subset runs now emit JSON result records and baseline comparison surfaces routing/recovery regressions without touching protected planning docs. files changed: `src/benchmark-runner.ts`, `src/run-benchmarks.ts`, `test/benchmark-runner.test.ts`, `.ralph/items.json`, `.ralph/progress.md`. verification: `npm run build` ✅, `npm test` ✅, `node dist/run-benchmarks.js --provider local --model seeded --evals-path .pi/evals/ptc --cases recovery-missing-await --baseline <tmp>/baseline.json --results-path <tmp>/results.json --timestamp 2026-03-16T00:00:00.000Z` ✅ (record fields present; comparison reported routing/recovery/success regressions). next: add regression coverage around routed eval prompts and recovery-sensitive cases now that benchmark infrastructure exists.
- item: regression coverage. decisions: blocked mutation-prompt recovery at request start with a prompt-level gate in `src/index.ts` and added an integration test proving mutation prompts neither auto-route nor arm async auto-recovery; kept existing routing/classifier/clamp coverage unchanged. files changed: `src/index.ts`, `src/utils.ts`, `test/index.test.ts`, `.ralph/items.json`, `.ralph/progress.md`. verification: `npm run build` ✅, `npm test` ✅. next: update user-facing docs for the bounded async-only recovery model and JSON eval workflow.
